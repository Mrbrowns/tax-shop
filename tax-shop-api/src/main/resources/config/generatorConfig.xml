<generatorConfiguration>

    <context id="DB2Tables" targetRuntime="MyBatis3" defaultModelType="flat">
        <plugin type="org.mybatis.generator.plugins.RenameExampleClassPlugin">
            <property name="searchString" value="[e|E]xample$" />
            <property name="replaceString" value="Criteria" />
        </plugin>

        <commentGenerator>
            <property name="suppressDate" value="true" />
        </commentGenerator>

        <jdbcConnection driverClass="com.mysql.jdbc.Driver"
                        connectionURL="jdbc:mysql://localhost:3306/mysql" userId="root"
                        password="root">
        </jdbcConnection>

        <javaTypeResolver>
            <property name="forceBigDecimals" value="false" />
        </javaTypeResolver>

        <!-- generate Model -->
        <javaModelGenerator targetPackage="com.yhsoft.api.entity"
                            targetProject="${entity.target.dir}">
            <property name="enableSubPackages" value="true" />
            <property name="trimStrings" value="true" />
        </javaModelGenerator>



        <!-- 自动生成xml（但是好像一直没有生成功，反正也不用xml） -->
        <sqlMapGenerator  targetPackage="com.yhsoft.api.mapper"
                          targetProject="${dao.resources.dir}">
            <property name="enableSubPackages" value="true" />
        </sqlMapGenerator>

        <!-- 自动生成mapper接口, 可以是 ANNOTATEDMAPPER(注解), XMLMAPPER(xml), MIXEDMAPPER(不知道) -->
        <javaClientGenerator type="ANNOTATEDMAPPER"
                             targetPackage="com.yhsoft.api.mapper" implementationPackage="com.yhsoft.api.mapper.impl"
                             targetProject="${entity.target.dir}">
            <property name="enableSubPackages" value="true" />
        </javaClientGenerator>

        <table tableName="Job" domainObjectName="Job" ></table>
    </context>
</generatorConfiguration>